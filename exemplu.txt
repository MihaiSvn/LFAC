class_section

class MyClass
{
    class_var_section
        int a=10;
        float b;
        bool x;
        int sir[10];

    class_methods_section
        int method(int c,bool ds){
            bool terog;
        }
        bool print(){
            //Print(12);
        }

}

class TwoClass{
    class_methods_section
        bool isWorking(){
            //Print(123);
        }
}

global_var_section
    string wow;
    int pa;
    int a;
    int b;
    bool sd;
    int tesst1,test2,test3;
    int vect[5];
    int vector[3]={1,2,3};
    char car;
    bool conditions[2]={true,false};

global_fun_section
    bool check(int s,int p)
    {
        //Print(ITWORKSINFUNCTION);
    }
    int try(bool as)
    {
        int x;
    }
    bool checknull(int a){

    }

main
    a=b;
    a=10;
    MyClass idk = new MyClass();

    idk.a++;
    idk.sir[0]=10;
    vect[3]=12+2+12;
    sd=true&&false;
    check(tesst1,pa);
    //wow.method(a,b);
    //wow.a = 2+4+wow.method(a,b)+10;
    //function(wow.method(function(a,b),b));

    //check();
    Print(2);
    if(a>b){
        //Print(YES);
    }

    if(idk.print()==checknull(a)){
        //Print(YAY);
    } else if(idk.method(vect[2],true)==idk.a) {
        Print(2);
    }

    while(a!=b){
        a=a+1;
    }

    if(a==b){
        TwoClass w = new TwoClass();
        //w.item++;
    } else if(a>b){
        while(a>b){
            a--;
        }
    } else {
        a=b;
        //a=w.getItem()++;
    }

    /*
    sdasdawd ssigmsda 
    */

    //ssigmsda

    a=b+2++;
    Print(10);
    Print(true);
end_main